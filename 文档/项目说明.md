# MaskFlow 项目说明

MaskFlow 是一个面向图像数据集的 AI 自动标注平台，包含 Vue 前端、ASP.NET Core 业务 API、Python SAM 推理服务，以及原型图和模型资源。

## 目录结构

```text
src/
  frontend/maskflow-web/       Vue 3 + Vite 前端
  backend/maskflow-api/        ASP.NET Core WebAPI 后端
  ai/sam-inference/            Python GPU 推理服务
  legacy/                      旧版代码备份

文档/
  项目说明.md
  产品设计/

资源/
  模型/sam3.pt
  原型图/

docker-compose.yml             Docker 编排入口
.gitignore                     忽略规则
```

## 本地开发

业务 API：

```powershell
dotnet run --project src\backend\maskflow-api\MaskFlow.Api.csproj --urls http://127.0.0.1:8010
```

前端：

```powershell
cd src\frontend\maskflow-web
npm install
npm run dev -- --host 127.0.0.1 --port 3010
```

SAM 推理服务：

```powershell
cd src\ai\sam-inference
python -m uvicorn app.main:app --host 127.0.0.1 --port 8001
```

## Docker 启动

```powershell
docker compose up -d --build
```

Docker 中的模型挂载路径来自：

```text
资源/模型/sam3.pt
```

## 后端接口

调试界面：

```text
http://127.0.0.1:8010/swagger
```

业务 API 保持原有路径不变，例如：

```text
POST /api/auth/register
POST /api/auth/login
GET  /api/me
GET  /api/billing/plans
POST /api/files/upload
POST /api/segment
POST /api/annotation/masks
GET  /api/tasks
POST /api/export/dataset
```

ComputeHub 风格接口保留在 `/v1` 下：

```text
/v1/jobs
/v1/nodes
/v1/pools
/v1/pricing
/v1/wallet/balance
/v1/settlements
```
